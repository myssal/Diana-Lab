<UserControl x:Class="DianaLab.GUI.Views.Live2DAssetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DianaLab.GUI.Views"
             xmlns:converters="clr-namespace:DianaLab.GUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:TagListToStringConverter x:Key="TagListToStringConverter"/>
        <converters:TagToBrushConverter x:Key="TagToBrushConverter"/>
        <Style TargetType="DataGridCell">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem Header="Edit" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=DataContext.EditAssetCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}, Path=PlacementTarget.DataContext}"/>
                        <MenuItem Header="Delete Entry" IsEnabled="False"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding}" Grid.Column="0" />
                            <Image x:Name="SortIcon" Width="12" Height="12" Grid.Column="1" Margin="5,0,0,0" Visibility="Collapsed" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridColumnHeader}}, Path=SortDirection}" Value="Ascending">
                                <Setter TargetName="SortIcon" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortIcon" Property="Source" Value="F:/FullSetC/Tool/Game/Brown Dust/ExtractAsset/DianaLab.GUI/Resources/Icons/up.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridColumnHeader}}, Path=SortDirection}" Value="Descending">
                                <Setter TargetName="SortIcon" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortIcon" Property="Source" Value="F:/FullSetC/Tool/Game/Brown Dust/ExtractAsset/DianaLab.GUI/Resources/Icons/down.png"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>  
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            
            
            
            <Button Grid.Column="1" Content="Add.." Margin="5,5" Click="AddButton_Click"/>
            <Grid Grid.Column="2">
                <TextBox x:Name="SearchTextBox" Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" FontSize="13"/>
                <Popup IsOpen="{Binding IsSuggestionsOpen}" StaysOpen="False" Placement="Bottom" PlacementTarget="{Binding ElementName=SearchTextBox}" AllowsTransparency="True">
                    <Border CornerRadius="5" Background="White" BorderBrush="Gray" BorderThickness="1">
                        <ListBox ItemsSource="{Binding SearchSuggestions}"
                                 Width="200"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="13" TextTrimming="CharacterEllipsis">
                                        <TextBlock.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                          Command="{Binding DataContext.SelectSuggestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </TextBlock.InputBindings>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Popup>
            </Grid>
            <Button Grid.Column="3" Content="Search" Margin="5,5" Command="{Binding SearchCommand}"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <DataGrid Margin="5,0,5,5" AutoGenerateColumns="False" ItemsSource="{Binding L2DAssets}" IsReadOnly="True" CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="*" SortMemberPath="id"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding name}" Width="5*" SortMemberPath="name"/>
                    <DataGridTextColumn Header="L2D" Binding="{Binding l2d}" Width="3*" SortMemberPath="l2d"/>
                    <DataGridTemplateColumn Header="Tag" Width="3*" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding l2dTags}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="5" Margin="2" Padding="5,2" Background="{Binding Converter={StaticResource TagToBrushConverter}}">
                                                <TextBlock Text="{Binding}" Foreground="Black"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Release Date" Binding="{Binding releaseDate}" Width="2*" SortMemberPath="releaseDate"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>