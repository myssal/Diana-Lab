<UserControl x:Class="DianaLab.GUI.Views.Live2DAssetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DianaLab.GUI.Views"
             xmlns:converters="clr-namespace:DianaLab.GUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:TagListToStringConverter x:Key="TagListToStringConverter"/>
        <converters:TagToBrushConverter x:Key="TagToBrushConverter"/>
        <Style TargetType="DataGridCell">
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>  
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="1" Content="Add.." Margin="5,5" Click="AddButton_Click"/>
            <TextBox Grid.Column="2" Width="200" Text="Search" Margin="0,5"/>
            <Button Grid.Column="3" Content="Search" Margin="5,5"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <DataGrid Margin="5,0,5,5" AutoGenerateColumns="False" ItemsSource="{Binding L2DAssets}" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="*"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding name}" Width="5*"/>
                    <DataGridTextColumn Header="L2D" Binding="{Binding l2d}" Width="3*"/>
                    <DataGridTemplateColumn Header="Tag" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding l2dTags}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="5" Margin="2" Padding="5,2" Background="{Binding Converter={StaticResource TagToBrushConverter}}">
                                                <TextBlock Text="{Binding}" Foreground="Black"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Release Date" Binding="{Binding releaseDate}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>